from model.player import Player, PlayerRepository

repository = PlayerRepository()

#
# def register_new_player(firstname: str, lastname: str, birth: str, national_chess_id: str):
#     player = Player(firstname, lastname, birth, national_chess_id)
#     repository.add_player(player)
